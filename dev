pipeline {
     agent any 
     stages {
          stage ('this is build') {
              steps {
                  echo 'build run'
                  }
               }
               stage ('this is test') {
                   steps {
                      echo 'test'
                      }
                   }
            }
         }
